node_modules
dist
.env
.env.local
.netlify
*.log
netlify/functions/mail-config.json

# test artifacts (raw results / generated report — not source; Playwright's own
# trace/screenshot output lives outside the repo entirely, see playwright.config.ts)
test-results
playwright-report
orbit-feature-test-report.pdf
