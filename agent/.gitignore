# Local Netlify folder
.netlify

# Per-machine secrets — never commit these
gmail-config.json
pg-config.json
agent-config.json

# Packaged app build output (large binary, rebuild with `npm run build:exe`)
dist/
