// Permanent link — GitHub always resolves /releases/latest/download/<asset>
// to the newest published release's asset of that name, so this URL never
// changes across rebuilds as long as future releases keep naming it orbit.exe.
export const ORBIT_AGENT_DOWNLOAD_URL =
  "https://github.com/geekysaquib/orbitdev.work/releases/latest/download/orbit.exe";
